console.log('****************** Loop 20 *********************');

/* Loop over the below object. For each value (which is an array), loop over it and console log each item.

For the first loop over the object, the following lines should be console logged:

In the first array, item at index 0 is 10
In the first array, item at index 1 is 200
In the first array, item at index 2 is 30

*/



var objectOfArrays = {
  first: [10,200,30],
  second: ['Hi', 'Bye'],
  third: ['Hey', 'Ho'],
  fourth: [13, 99, 102]
};
