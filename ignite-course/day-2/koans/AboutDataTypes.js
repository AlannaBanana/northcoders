describe('About Data Types', function() {

  it('should expect true', function() {
    // Your journey begins here: Replace the word false with true
    expect(false).toBeTruthy();
  });

  it('should expect a number', function() {
    var num = FILL_ME_IN;
    expect(typeof num).toEqual('number');
  });

  it('should expect a string', function() {
    var str = 3; // <- FILL_ME_IN
    expect(typeof str).toEqual('string');
  });

  it('should expect a boolean', function() {
    var actualValue = true;
    var expectedValue = FILL_ME_IN;
    expect(actualValue).toEqual(expectedValue);
  });

  it('should expect a boolean', function() {
    var actualValue = false;
    var expectedValue = FILL_ME_IN;
    expect(actualValue).toEqual(expectedValue);
  });

  it('should expect the value null', function() {
    var actualValue = null;
    var expectedValue = FILL_ME_IN;
    expect(actualValue).toEqual(expectedValue);
  });

  it('should expect the value undefined', function() {
    var actualValue = undefined;
    var expectedValue = FILL_ME_IN;
    expect(actualValue).toEqual(expectedValue);
  });

});
