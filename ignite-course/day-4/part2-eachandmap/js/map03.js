console.log('****************** map 3 *********************');

// Create the hitBall variable. It should be a function that takes one argument.
// If the argument is 'ping' it should return 'pong', and vice versa.

var hitBall = function () {

};

var result = ['ping', 'pong', 'pong', 'ping', 'ping', 'pong'].map(hitBall);
