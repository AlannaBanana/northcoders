console.log('****************** forEach 4 *********************');

// call forEach on the following array. For each number, double it and push it into the results array. Then console log the results array.

var arr = [2, 4, 6];
var results = [];
